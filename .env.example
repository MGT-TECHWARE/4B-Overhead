# GEMINI_API_KEY: Required for Gemini AI API calls.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
APP_URL="MY_APP_URL"

# === Contact form (Cloudflare Pages Function: functions/api/contact.js) ===
# These are configured in the Cloudflare Pages dashboard, not in a local .env.
#
#   Cloudflare Pages → 4b-overhead-demo → Settings → Environment Variables
#     GMAIL_USER          = coltenbeaty182@gmail.com
#     GMAIL_APP_PASSWORD  = ryxw qytg asyf orgs   (16-char Gmail app password)
#
# The Pages Function uses cloudflare:sockets to talk SMTP to smtp.gmail.com:465
# directly — no separate Node server is needed.
